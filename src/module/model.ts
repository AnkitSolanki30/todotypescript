import { Key } from "react";

export interface Todo {
    // __v: number;
    _id: Key ;
    checked: boolean;
    content: string;
    // createdAt: string;
    // updatedAt: string;
    // user: string;
}